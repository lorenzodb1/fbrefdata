[project]
name = "fbrefdata"
version = "1.0.0"
description = "Scrape soccer data from FBref."
authors = [
    {name = "lorenzodb1"}
]
license = {text = "Apache-2.0"}
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
